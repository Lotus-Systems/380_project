#include <iostream>

/*
 * functionality:
 * only really need to impliment functions that interact with other classes or the DB
 * DB can be just saving and loading from a local file, don't actually have to host SQL locally.
 * GUI just impliments the functions and displays the data, doesn't have to look anything like the mockup.
 *
 * do the association via id, not by pointer.
 * 
 * testing
 */

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
